# D42 — Управление подписками

Маршрут: `/admin/subscriptions`

Статус: route-specific аудит подтверждает существующий mobile-first redesign.

## Продуктовая опора

`docs/product/prd.md`, §§8–8.1 и 11: администратор вручную задаёт план, срок, grace period и лимиты. Bronly не проводит онлайн-оплату в MVP.

## Решение и QA

`AdminSubscriptionsPage` сохраняет фильтры, disclosure rows, focus query, форму сохранения и feedback. Проверены `1440 × 900`, `390 × 844` и `320 × 720`: поля 2→1 колонка, длинные имена переносятся, mobile actions остаются над bottom navigation. Loader и save action не меняются.
