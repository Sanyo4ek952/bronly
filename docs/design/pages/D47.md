# D47 — Публичная подборка

Маршрут: `/c/[slug]`

Статус: route-specific аудит подтверждает существующий public redesign.

## Продуктовая опора

`docs/product/prd.md`, §§5.3–5.5, 7 и 15: подборка доступна только по прямой ссылке, содержит выбранные варианты и не является общим каталогом; заявка всегда относится к конкретному номеру.

## Решение и QA

Маршрут переиспользует public header, hero, stay filter и room browser, сохраняет `noindex`, creator-specific цену/контакты, unavailable states и open tracking. Проверены `1440 × 900`, `390 × 844` и `320 × 720`: карточки и CTA складываются в один столбец, контекст персональной подборки остаётся явным.
