# D36 — Агентские заявки

Маршрут: `/agent/dashboard/requests`

Статус: канонический контракт восстановлен; реализация и QA ранее выполнены в исторической D23.

## Продуктовая опора

`docs/product/prd.md`, §§5.4–5.5, 6, 7 и 15: агентская заявка передаётся владельцу только явным действием агента; `completed` ставит владелец, ценовой snapshot сохраняется.

## Решение и QA

Открытый H1, обязательное пояснение процесса, query notice и сводка `все / новые / переданные`; карточки сохраняют контакты, snapshot и server actions. Route-specific QA уже выполнен на `1440 × 900`, `390 × 844` и `320 × 720` в исторической D23: summary, карточки и actions читаемы без page-level overflow.
