# Статус переработки Bronly

Допустимые статусы: pending, in_progress, done, blocked.

| № | Задача | Статус | Результат и проверки |
|---:|---|---|---|
| 01 | Техническая база | done | 2026-09-14: инструкция local-development.md, исправлена supabase-setup.md; lint, tsc, build (49/49), encoding — exit 0; чистый npm ci — 372 пакета; Graphify src актуален (314 файлов, 1604 узла, 5214 связей). Подробности ниже. |
| 02 | Синхронизация продукта | done | 2026-09-14: решения о URL агента, самостоятельных номерах и ручных рефералах перенесены в PRD; границы продукта и журнал согласованы; задачи 03–15 привязаны к разделам PRD; encoding — exit 0 (376 файлов), ручная сверка терминов пройдена. |
| 03 | Тестовая основа | done | 2026-09-14: node:test без новых зависимостей, 54/54 теста; чистые правила подписки и переходов заявок; исправлены несуществующие даты и пустые интервалы; tsc, целевой ESLint, encoding — exit 0; Graphify обновлён. |
| 04 | Календарь и цены | done | 2026-09-14: включительные занятые даты согласованы в UI/server/SQL; однодневная занятость блокирует ночь; pricing snapshot использует единый расчет; 58/58 тестов, tsc, ESLint, encoding — exit 0; Graphify обновлен. |
| 05 | Ошибки и mock-режим | done | 2026-09-14: запись заявок и мутации работают fail-closed; demo включается только через `BRONLY_DEMO_MODE=true`; публичные страницы, кабинеты и админка различают пустые данные и недоступность сервиса; 62/62 теста, tsc, целевой ESLint, encoding, diff-check — exit 0; ручной smoke без Supabase пройден; Graphify обновлён. |
| 06 | Supabase и права доступа | done | 2026-09-14: клиенты используют сгенерированный `Database`; server actions и RLS проверяют роль/владение, включая отдельные номера; публичная заявка валидируется на сервере; 67/67 тестов, локальный RLS smoke, tsc, ESLint, build (49/49), encoding и diff-check — exit 0; Graphify обновлён. |
| 07 | Tailwind UI-основа | done | 2026-09-14: shared UI-примитивы и каркасы владельца/администратора перенесены на Tailwind CSS v4; состояния и a11y унифицированы; tsc, ESLint, build (49/49), encoding и diff-check — exit 0; wide/mobile visual smoke пройден; Graphify обновлён. |
| 08 | Основной сценарий владельца | done | 2026-09-14: вход, онбординг, кабинет, единый список объектов и самостоятельных номеров, формы, фото, цены, календари и публичная ссылка перенесены на Tailwind; 67/67 тестов, tsc, ESLint, build (49/49), encoding и diff-check — exit 0; mobile/desktop owner-flow smoke пройден; Graphify обновлён. |
| 09 | Публичная страница и заявка | done | 2026-09-15: owner-public page, фильтр, выбор номера, форма и success/error состояния перенесены на Tailwind; серверный owner-контекст и snapshot усилены и покрыты тестами; 52 целевых теста, tsc, ESLint, build (49/49), encoding и diff-check — exit 0; desktop/mobile guest-flow smoke пройден; Graphify обновлён. |
| 10 | Подписка | done | 2026-09-15: единый pure runtime trial/active/grace/expired без write-on-read; owner/agent лимиты и public/request согласованы; DB-guard активных номеров и атомарное ручное продление с audit log; 31 целевой тест, SQL smoke, tsc, ESLint, build (49/49), encoding и browser smoke — пройдены; Graphify обновлён. |
| 11 | Агентский контур | done | 2026-09-15: полный proposal → active/declined → заявка → передача → запрос завершения → completed; 45 целевых тестов, RLS smoke, tsc, ESLint, build (49/49), encoding, diff-check и browser smoke — пройдены; Graphify обновлён. |
| 12 | Коллекции | done | 2026-09-15: owner/agent коллекции, guest title, состав property/room, публичная витрина, актуальная цена, snapshot заявки и deduplicated статистика; 90/90 unit-тестов, SQL smoke, tsc, ESLint, build (49/49), encoding, diff-check и browser smoke — пройдены; Graphify обновлён. |
| 13 | Уведомления и PWA | done | 2026-09-15: in-app + idempotent fan-out в push/Telegram, delivery-status и диагностика, browser permissions/subscription lifecycle, installable manifest/SW; 95/95 unit-тестов, tsc, ESLint, build (50/50), encoding, diff-check и browser PWA smoke — пройдены; Graphify обновлён. |
| 14 | Рефералы и админка | done | 2026-09-15: role-safe приглашения и регистрация, автоматические целевые действия, атомарная ручная модерация и ровно +10 дней во всех owner/agent контекстах пригласившего; админские hide/freeze и UI на Tailwind; 98/98 unit-тестов, SQL smoke, tsc, ESLint, build (50/50), encoding, diff-check и browser smoke — пройдены; Graphify обновлён. |
| 15 | Очистка и финальная проверка | done | 2026-09-15: нарушения направления слоёв, живые `br-*`, пустые catch и небезопасные `as any/unknown/never` отсутствуют; orphan UI/mock удалены, globals сокращён до 319 строк; 98/98 unit, 5/5 RLS smoke, lint, tsc, build (50/50), encoding, diff-check и mobile/desktop browser smoke — пройдены; Graphify обновлён. |

## Текущая точка

- Текущая задача: последовательность 01–15 завершена.
- Последнее подтвержденное состояние: все задачи переработки завершены и проверены; следующей pending-задачи нет.
- Блокеры: отсутствуют.


## Результат задачи 01 — 2026-09-14

- Окружение: Windows / PowerShell, Node.js 22.23.1, npm 10.9.8. Версии lockfile: Next 16.2.7, React 19.2.7, TypeScript 6.0.3, Tailwind 4.3.1, ESLint 9.39.4.
- Проверки: `npm run lint` — exit 0, без замечаний; `npx --no-install tsc --noEmit --incremental false` — exit 0; `npm run build` — exit 0, компиляция 8.9 s, генерация 49/49 страниц; `npm run check:encoding` — exit 0, 376 отслеживаемых текстовых файлов. Новые документы дополнительно проверены напрямую на UTF-8 без BOM.
- Воспроизводимость установки: `npm ci --prefix <temporary-directory> --no-audit --no-fund` с копиями package.json и package-lock.json — exit 0, 372 пакета за 24 s. Временная установка не меняла рабочий node_modules или lockfile.
- Graphify: `graphify update ./src` — exit 0, AST-only без LLM; 314 файлов, 1604 узла, 5214 связей, топология без изменений. Для навигации выполнен query по Supabase / requests / calendar / pricing. Граф игнорируется git.
- Документы: `docs/engineering/local-development.md` содержит установку, env-переменные, команды, структуру, конфигурацию и карту долга; `docs/engineering/supabase-setup.md` исправляет ссылки на PRD и новые пути data layer, описывает mock-ограничения и разрушительную первую миграцию.
- Крупные файлы: globals.css — 8798 строк; admin-sections.tsx — 1013; owner-calendar-browser.tsx — 672; public-collection-data.ts — 625; public-agent-data.ts — 511; public-property-data.ts — 493.
- Подтверждённые чтением кода проблемы: включительная граница занятых дат в owner UI против строгой в публичной проверке пересечения; mock-успех заявки без Supabase; catch с возвратом null в публичном data layer; ручные Supabase Row-типы и service-role доступ без Database generic; смешение br-* и Tailwind; отсутствие тестового runner.
- Supabase: 20 миграций, первая содержит DROP SCHEMA public CASCADE; локального config.toml нет. Миграции, внешняя база, RLS и доставка уведомлений в задаче 01 не запускались и не подтверждены. Сборка использовала существующий .env; секреты не выводились и не изменялись.
- Ограничения окружения: sandbox запрещал запись файлов и запуск некоторых дочерних процессов (Graphify / git в encoding); разрешённое локальное выполнение устранило ошибки. Блокеров нет.
- Сохранность: исходный git status содержал только неотслеживаемую docs/rebuild-execution/. Её файлы сохранены, кроме порученного обновления STATUS.md; исходники, конфигурация и зависимости не менялись.
- Следующая задача: 02 «Синхронизация продукта», файл `docs/rebuild-execution/tasks/02-product-sync.md`. Её реализация в текущем чате не начиналась.
## Результат задачи 02 — 2026-09-14

- На момент выполнения были изменены только продуктовые документы, README плана и этот статус. Исходные изменения задачи 01 и остальные файлы плана сохранены. Действующий документ границ теперь находится в `docs/product/product-boundaries.md`.
- Решение 2026-06-05: PRD 5.4 закрепляет автоматическую стабильную ссылку `/a/ag_xxxxxx`, отсутствие ручного редактирования, редиректы старых ссылок и техническое сохранение legacy slug.
- Решение 2026-06-07: PRD 5.7 закрепляет самостоятельный номер, собственные адресные поля и тип размещения, единый список владельца, отдельный публичный блок и поддержку агентского контура. Согласованы онбординг 5.2, роли 4.2 и учет всех активных номеров в разделе 8; сохранена заявка на room_id с необязательным property_id в разделе 6.
- Решение 2026-06-11: PRD 5.8 и 8.1 закрепляют персональное приглашение, пригласившего и ожидаемую роль, связь при регистрации, целевые действия владельца/агента, очередь pending, ручное подтверждение или отклонение 10 дней и применение ко всем owner/agent контекстам подписки пригласившего. Согласованы роли, регистрация, план и критерии готовности.
- В decision-log помечен перенос всех трех датированных решений со ссылками на разделы. В документе границ отражены принятые дополнения. В README плана добавлена таблица продуктовой опоры задач 03–15; порядок очереди сохранен.
- Ручная сверка: роли и запрет редактирования чужих данных, room_id и передача агентской заявки, состав/срок жизни/цены/статистика коллекций, snapshot и расчет по ночам, подписка и терминология согласованы между PRD, журналом и краткими документами. В PRD 20 уточнено, что рекомендации о коллекциях уже включены в существующие требования 5.6. Новых продуктовых решений и неразрешенных конфликтов нет.
- Проверки: `npm run check:encoding` — exit 0, 376 отслеживаемых текстовых файлов; первоначальный запуск в sandbox получил `spawnSync git EPERM`, повтор с разрешенным локальным выполнением успешен. Все пять измененных документов дополнительно проверены напрямую, включая неотслеживаемые README и STATUS: строгий UTF-8 без BOM и без mojibake. Ручная сверка по `docs/product/terminology.md` пройдена; пользовательские формулировки остаются языком заявок, запрещенные выражения остаются только в запретах.
- Graphify не запускался: задача ограничена текстом документации, исходники и связи модулей не менялись. Сборка, lint и тесты не требовались по задаче 02.
- Блокеры: отсутствуют. Следующая задача: 03 «Тестовая основа», файл `docs/rebuild-execution/tasks/03-test-foundation.md`; ее реализация в этом чате не начиналась.

## Результат задачи 03 — 2026-09-14

- Продуктовая опора: PRD 6, 7, 8 и 17. Добавлен `npm run test:unit`: встроенный Node.js test runner с TypeScript type stripping, без новых зависимостей, без сборки и без изменения lockfile. В `tsconfig.json` разрешены `.ts`-импорты при существующем `noEmit`; инструкция добавлена в `docs/engineering/local-development.md`.
- Три файла `tests/unit/*.test.ts`: 54 теста, включая матрицу 125 пар статусов для пяти источников/контекстов заявки и 15 вариантов передачи агентом. Проверены ночи, високосный год и календарные границы, базовые/сезонные цены, последняя ночь сезона, исключение даты выезда, агентская наценка и округление, соседние/пересекающиеся интервалы, параметры проживания, ограничения номеров и точные границы active/grace/expired.
- `subscription/model/subscription-rules.ts` содержит извлечённое вычисление runtime state с явным `now`; серверный слой использует эту функцию и сохраняет прежние экспорты. В `request/api/request-rules.ts` извлечены проверки перехода владельцем и передачи агентом; серверные мутации вызывают их после прежних проверок пользователя и доступа. Продуктовые правила переходов и подписки сохранены.
- Тесты сначала воспроизвели два дефекта `pricing.ts`: дата `2026-02-30` незаметно превращалась в мартовскую, нулевой интервал внутри занятых дат считался пересечением. Минимальные исправления проверяют точное соответствие разобранной даты и положительную длину обоих интервалов. Соответствующие регрессионные тесты проходят.
- Проверки: `npm run test:unit` — exit 0, 54 passed / 0 failed / 0 skipped; `npx --no-install tsc --noEmit --incremental false` — exit 0; ESLint только пяти изменённых TS-модулей и трёх файлов тестов — exit 0; `npm run check:encoding` — exit 0, 376 отслеживаемых файлов. Все 12 файлов задачи, включая новые тесты, модель и документы, дополнительно проверены напрямую на строгий UTF-8 без BOM и mojibake.
- Graphify: перед навигацией `graphify update ./src` и query по pricing/subscription/request; после изменений повторный AST-only update — exit 0, 315 файлов, 1608 узлов, 5245 связей, 66 сообществ. Минимальный общий scope — src, внешняя semantic extraction не использовалась.
- Ограничения проверки: unit-тесты не подтверждают RLS, подсчёт номеров в базе, сохранение заявки, уведомления или JSX. Существующая включительная граница сезона и тарифные пороги защищены как текущее поведение. Согласование календарного UI остаётся задачей 04, дальнейшая работа с подпиской — задачей 10. Production build по задаче 03 не требуется и не запускался.
- Окружение: sandbox запрещал запись через reparse point и запуск дочерних процессов Graphify/Node; разрешённое локальное выполнение успешно. Node выводит информационное предупреждение `MODULE_TYPELESS_PACKAGE_JSON`, описанное в инструкции; тип модулей приложения сохранён.
- Предыдущие изменения задач 01–02 сохранены. Блокеров нет. Следующая задача: 04 «Календарь и цены», файл `docs/rebuild-execution/tasks/04-calendar-pricing.md`; её реализация в этом чате не начиналась.

## Результат задачи 04 — 2026-09-14

- Продуктовая опора: PRD 4.1–4.3, 5.3, 6 и 7. Занятость хранится как включительный диапазон `[starts_on, ends_on]`: одинаковые даты означают один занятый день. Проживание хранится как `[check_in, check_out)`, поэтому дата выезда не считается ночью проживания.
- В `room/model/date-ranges.ts` вынесены строгий разбор ISO-даты, проверка включительного диапазона, попадание даты, пересечение двух календарных диапазонов и пересечение проживания с занятостью. Клиентские calendar helpers, серверная доступность и формы создания/изменения занятости и сезонной цены используют эти правила.
- Исправлен основной дефект: занятость с одинаковыми `startsOn` и `endsOn` теперь блокирует проживание в эту ночь; выезд в первый занятый день и новый заезд на следующий день после последнего занятого остаются допустимыми.
- Серверное сохранение заявки передает агентскую наценку в общий `calculateRoomPricing` и сохраняет `total_price`, среднюю цену и `nightly_prices` из одного результата. Итог равен сумме округленных до копеек ночей, включая граничный случай, где повторный расчет после snapshot расходился на копейку.
- Добавлена additive migration `202609140001_align_calendar_pricing_boundaries.sql`: документированы границы дат; исправлен exclusion range сезонных цен без лишнего дня; advisory-lock triggers запрещают новые пересекающиеся диапазоны занятости и заявки на занятые ночи; pricing trigger синхронизирует итог и nightly snapshot на записи заявки. Существующие диапазоны не переписываются.
- Регрессии: новый файл `calendar-pricing-boundaries.test.ts` и обновленная матрица pricing проверяют однодневную занятость, соседние даты, включительные границы сезона, исключение checkout и сумму агентских округленных ночей.
- Проверки: `npm run test:unit` — exit 0, 58 passed / 0 failed / 0 skipped; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint девяти TS-файлов — exit 0; `npm run check:encoding` — exit 0, 376 отслеживаемых файлов; 10 файлов задачи дополнительно проверены как строгий UTF-8 без BOM; `git diff --check` — exit 0.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 316 файлов, 1613 узлов, 5276 связей, 62 сообщества. Первоначальный sandbox-запуск был заблокирован, разрешенный локальный запуск успешен.
- Ограничение проверки migration: локальный Supabase CLI отсутствует (`npx --no-install supabase --version` получил `ENOTCACHED`), поэтому migration не применялась к локальной базе; выполнено статическое ревью SQL. Docker доступен как бинарник, но отдельная Supabase/Postgres среда в проекте не настроена.
- Предыдущие изменения задач 01–03 сохранены. Блокеров нет. Следующая задача: 05 «Ошибки и mock-режим», файл `docs/rebuild-execution/tasks/05-runtime-errors.md`; её реализация в этом чате не начиналась.

## Результат задачи 05 — 2026-09-14

- Продуктовая опора: PRD 6, 11, 15 и 17. Запись публичной заявки, смена статуса, передача владельцу, завершение агентом и мутации сотрудничеств при недоступном Supabase возвращают явную ошибку, а не ложный успех. После уже сохранённой заявки сбой вторичной доставки уведомления не превращает результат в ошибку и не провоцирует повторную запись.
- Demo-режим отделён от ошибок: `BRONLY_DEMO_MODE=true` — единственный способ включить демонстрационные данные для поддерживаемого demo slug. Отсутствующая конфигурация, ошибка запроса или случайное исключение больше не включают mock автоматически. Переменная и поведение описаны в `.env.example`, `local-development.md` и `supabase-setup.md`.
- Публичные страницы владельца, агента и коллекции возвращают отдельное состояние `service_unavailable`; форма заявки показывает, что данные не отправлены. Кабинеты владельца и агента, заявки, сделки, календарь, возможности, сотрудничества и админка показывают явное состояние недоступности вместо mock-метрик или правдоподобных пустых списков.
- Критические чтения Supabase проверяют `error`; широкие fallback в auth/profile, заявках, подписке, уведомлениях, сотрудничествах и административной сводке заменены fail-closed поведением. Добавлены безопасные структурированные diagnostics: только область, событие, тип ошибки, ограниченный код и числовой status — без provider message, stack trace, секретов и пользовательских полей.
- Регрессии: `tests/unit/runtime-errors.test.ts` проверяет opt-in demo, публичное отображение инфраструктурной ошибки и фильтрацию diagnostics. `npm run test:unit` — 62 passed / 0 failed / 0 skipped; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint изменённых TS/TSX-файлов — exit 0; `npm run check:encoding` и прямая проверка новых файлов — exit 0; `git diff --check` — exit 0.
- Ручной smoke без Supabase выполнен на изолированной временной копии: страницы `/p/dom-u-morya`, формы заявки, агента и коллекции вернули HTTP 200 с явной недоступностью и без demo-объекта. При `BRONLY_DEMO_MODE=true` demo-объект показан явно. Отправка формы настоящим браузером завершилась `error=service`, показала «Данные не отправлены» и не открыла страницу успеха; серверный diagnostic не содержал введённые имя и телефон. Временные серверы остановлены, копия удалена; рабочий dev-сервер пользователя на порту 3000 не изменялся.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 319 файлов, 1635 узлов, 5419 связей, 66 сообществ. Внешняя semantic extraction не использовалась.
- Ограничение: отказ удалённой Supabase не симулировался на живом проекте и схема/RLS не менялись; проверены отсутствие конфигурации, кодовые ветки ошибок и безопасное отображение. Типизация базы и права доступа относятся к задаче 06.
- Предыдущие изменения задач 01–04 сохранены. Блокеров нет. Следующая задача: 06 «Supabase, типы и права доступа», файл `docs/rebuild-execution/tasks/06-supabase-access.md`; её реализация в этом чате не начиналась.

## Результат задачи 06 — 2026-09-14

- Продуктовая опора: PRD 4, 5.5, 5.7, 6, 13 и 14; зафиксированная логика ролей, владения, отдельных номеров и передачи заявок не менялась.
- Добавлен воспроизводимый `npm run supabase:types`: скрипт читает PostgREST OpenAPI настроенного проекта и формирует `src/shared/api/supabase/database.types.ts` для 25 таблиц. Browser, server-auth, admin и auth-confirm клиенты параметризованы `Database`; ручные cast удалены в основных потоках профиля, объектов, номеров, календаря, цен, заявок и подписки.
- Service role остаётся в модулях с серверной границей. Смешанные barrel-экспорты разделены в критическом client/edge пути: middleware читает публичное окружение напрямую, а клиентский `entities/room` больше не экспортирует серверный loader. Production build подтверждает отсутствие server-only кода в client bundle.
- Серверные owner actions до записи подтверждают роль, owner_id и точный scope номера (property_id или standalone). Переходы заявок владельца и агента дополнительно ограничены actor-aware trigger; публичная запись проверяет источник, владельца комнаты, property scope, обязательные поля, даты, вместимость и число номеров.
- Additive migration `202609140002_harden_data_access.sql` согласует RLS с server actions: чужие owner/agent записи недоступны, дочерние таблицы standalone room защищены через `rooms.owner_id`, агент читает календарь только при активной связи, новая агентская заявка скрыта от владельца до передачи.
- Проверки: `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint — exit 0; `npm run test:unit` — 67 passed / 0 failed; `npm run build` — exit 0, 49/49 страниц; `npm run check:encoding` — exit 0, 376 отслеживаемых файлов; `git diff --check` — exit 0. Информационное предупреждение Node о MODULE_TYPELESS_PACKAGE_JSON сохранено как известное.
- `tests/rls/access-smoke.sql` прошёл на изолированной временной PostgreSQL-базе после всей цепочки миграций: два владельца, агент, property room, standalone room и дочерние записи. Тестовая транзакция откатилась, временная БД удалена; существующая локальная база и удалённый Supabase не изменялись.
- Graphify использован для минимального scope `src`; финальный AST-граф обновлён после изменений. Предыдущие изменения задач 01–05 сохранены. Блокеров нет.
- Следующая задача: 07 «Tailwind UI-основа», файл `docs/rebuild-execution/tasks/07-tailwind-foundation.md`; её реализация в этом чате не начиналась.

## Результат задачи 07 — 2026-09-14

- Продуктовая опора: PRD 3, 9 и 15. Границы продукта, роли, тексты и сценарии не менялись; задача ограничена UI-основой и каркасами кабинетов.
- Унифицированы `Button` и `ButtonLink`: общие варианты и размеры, полноширинный режим, `disabled`, `loading`, `aria-busy`, блокировка недоступной ссылки и единый визуальный индикатор загрузки. Сохранена совместимость с прежним prop `loading`.
- `Input`, `Textarea` и `Select` получили единые hover/focus/error/disabled-состояния, стабильные id для подсказки и ошибки, `aria-describedby`, `aria-errormessage` и `role=alert`. API типов примитивов экспортирован через `shared/ui`.
- На локальные Tailwind utility-классы перенесены `Panel`, `InlineNotice`, `StatusPill`, `Tabs` и `BottomSheet`. Для вкладок добавлены tab-семантика, disabled-состояние и клавиатурная навигация Arrow/Home/End; для sheet сохранены drag-жест, safe-area, focus trap, Escape и возврат фокуса.
- Каркасы кабинетов владельца и администратора и три dashboard layout используют Tailwind для desktop/mobile навигации, активных, hover и focus-visible состояний. Shared-примитивы переиспользованы; новых компонентов не добавлялось. Индекс компонентов обновлён.
- Из `globals.css` удалены ставшие неиспользуемыми legacy-селекторы tabs, status pill, bottom sheet и двух shell; сохранены только используемый dashboard theme scope и keyframes sheet. Остальные глобальные стили не затрагивались.
- Проверки: `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint 16 TS/TSX-файлов — exit 0; `npm run build` — exit 0, компиляция 7.2 s и генерация 49/49 страниц; `npm run check:encoding` — exit 0, 376 отслеживаемых файлов; файлы задачи дополнительно проверены как строгий UTF-8 без BOM; `git diff --check` и аудит удалённых CSS-селекторов — exit 0.
- Визуальный smoke production-сборки пройден в браузере на 1440×1000 и 390×844: регистрация и лендинг не имеют переполнений или сломанной иерархии; мобильный `BottomSheet` корректно отображается, закрывается по Escape и после анимации возвращает фокус на кнопку открытия. Повторный runtime smoke подтвердил отсутствие передачи обработчиков из `ButtonLink` через границу Server/Client Component.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 321 файл, 1663 узла, 5526 связей, 75 сообществ. Предыдущие изменения задач 01–06 сохранены. Блокеров нет.
- Следующая задача: 08 «Основной сценарий владельца»; её реализация в этом чате не начиналась.

## Результат задачи 08 — 2026-09-14

- Продуктовая опора: PRD 4.1, 5.1–5.3, 5.7, 7 и 9. Бизнес-правила, границы продукта и терминология не менялись.
- Вход, регистрация, check-email и welcome собраны на новом переиспользуемом `AuthShell` с общими `Panel`, полями, кнопками и уведомлениями. Кабинет и чеклист владельца, быстрые действия и состояния пустых данных переведены на локальные Tailwind CSS v4 utility-классы.
- Объекты и самостоятельные номера представлены в одном адаптивном списке с одинаково доступными точками создания. Сохранены полные формы основных данных, публикации, контактов, фотографий, базовой и сезонной цены, занятых дат и локальных календарей. Общий календарь владельца пересобран как read-only обзор с фильтрами и переходами к точечному редактированию.
- Профиль владельца показывает персональный путь `/p/<slug>`, открытие и копирование ссылки; карточки Telegram и установки PWA приведены к той же UI-основе. Ошибки валидации, подписки, пересечений дат и инфраструктуры визуально отделены от успешных уведомлений.
- Переиспользование: мобильные фильтры инвентаря переведены на общий `BottomSheet` с узким `rootClassName`; удалены дублирующий sheet и неиспользуемые legacy-карточка/панель поиска. `AuthShell` добавлен в индекс компонентов. Старые селекторы только завершённых owner-экранов удалены из `globals.css`; используемые следующими задачами agent/public-селекторы сохранены.
- Проверки: `npm run test:unit` — 67 passed / 0 failed; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint изменённых TS/TSX/MJS-файлов — exit 0; `npm run build` — exit 0, генерация 49/49 страниц; `npm run check:encoding` — exit 0, 373 отслеживаемых текстовых файла; `git diff --check` и аудит удалённых owner legacy-селекторов — exit 0. Скрипт encoding узко исправлен, чтобы игнорировать tracked-файлы, уже удалённые из рабочего дерева.
- Browser smoke production-сборки пройден на 1280, 1024 и 390×844: login/register, кабинет, мобильный чеклист, единый инвентарь, создание объекта и самостоятельного номера, карточка и настройки номера, фотографии, сезонные цены, общий и локальный календари, профиль и публичная ссылка. Найденное горизонтальное переполнение toolbar на промежуточной ширине исправлено и перепроверено.
- Ограничение smoke: существующий локально авторизованный владелец имеет истёкшую подписку, поэтому внешние записи в настроенный Supabase и реальные mutation-submit не выполнялись. Доступные data-backed экраны, error-пути и server redirect/success mapping проверены; публичная страница корректно показала ограничение подписки. Новых секретов и production-данных не создавалось.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 320 файлов, 1653 узла, 5544 связи, 72 сообщества. Предыдущие изменения задач 01–07 сохранены. Блокеров нет.
- Следующая задача: 09 «Публичная страница и заявка гостя», файл `docs/rebuild-execution/tasks/09-public-request.md`; её реализация в этом чате не начиналась.

## Результат задачи 09 — 2026-09-15

- Продуктовая опора: PRD 4.3, 5.3, 5.7, 6, 7, 11, 15 и 17. Публичная страница остаётся персональной витриной конкретного владельца; общего каталога, обещания подтверждённого проживания и оплаты проживания не добавлено.
- Owner-public page, общие public header/hero/unavailable surfaces, браузер номеров, форма заявки и success-экран перенесены с `br-*` и inline styles на локальные Tailwind CSS v4 utilities с существующими токенами и shared `Button`, `Panel`, `Input`, `Select`, `InlineNotice`, `StatCard`. Новый переиспользуемый `PublicStayFilter` добавлен в существующий widget и индекс компонентов; shared-примитивы не расширялись.
- Фильтр дат, гостей и комнат работает для всей страницы владельца. Объекты и самостоятельные номера показаны отдельными блоками; подходящие варианты отделены от остальных, а причина несоответствия видна в карточке и форме. Исправлен ранний возврат data loader, из-за которого владелец только с самостоятельными номерами получал пустую витрину; demo больше не показывает неактивный номер.
- Server action повторно проверяет корректные целые значения и согласие на обработку данных. Чистое owner-правило фиксирует активность номера, принадлежность owner slug, скрытие профиля, публикацию/заморозку объекта, точный property/standalone scope и вместимость до проверки подписки, занятости и пересчёта цены. Цена из формы не принимается; snapshot формируется из серверных базовых/сезонных цен. Owner mapper по-прежнему показывает источник и предпочитает сохранённые snapshot total/nightly значения текущей цене номера.
- Проверки: 52 целевых теста заявки, source/access, snapshot, цены и доступности — passed; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint 18 файлов — exit 0; `npm run build` — exit 0, 49/49 страниц; `npm run check:encoding` — exit 0, 373 tracked-файла; прямая проверка 16 файлов как UTF-8 without BOM и `git diff --check` — exit 0.
- Browser smoke production-сборки пройден на desktop и mobile 390×844 без горизонтального переполнения: фильтр 01–04.07 / 4 гостя / 2 комнаты оставил подходящим конкретный семейный номер и показал 18 600 ₽ за 3 ночи; переход в форму сохранил `room_id` и параметры. Реальная локальная отправка без Supabase вернула `error=service` и «Данные не отправлены»; success-экран с тем же контекстом использует терминологию заявки и сообщает только о последующей связи владельца.
- Ограничение smoke: запись во внешнюю настроенную Supabase не выполнялась, чтобы не создавать тестовую production-заявку; success-state проверен напрямую после того, как server persistence/context/snapshot ветви были подтверждены кодом и целевыми unit-тестами. Локальный demo-сервер остановлен, временный viewport сброшен.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 320 файлов, 1659 узлов, 5567 связей, 76 сообществ. Legacy public CSS, который всё ещё используют прямые agent/collection страницы, сохранён до задач 11–12; завершённые owner/request компоненты от этих классов не зависят.
- Предыдущие изменения задач 01–08 сохранены. Блокеров нет. Следующая задача: 10 «Подписка», файл `docs/rebuild-execution/tasks/10-subscription.md`; её реализация в этом чате не начиналась.

## Результат задачи 10 — 2026-09-15

- Продуктовая опора: PRD 4.1–4.4, 5.3–5.7, 6, 8, 13–17. Зафиксировано единое поведение: trial/active/grace сохраняют кабинет, публичный контур и приём заявок; grace показывает предупреждение; expired оставляет кабинет для просмотра, но останавливает изменения, скрывает публичный контур и не принимает новые заявки. Онлайн-оплата проживания и автоматический billing не добавлены.
- `calculateSubscriptionRuntimeState` остаётся чистой и получает явное время. Trial теперь учитывает `trial_ends_at`, active — `paid_until`, grace — явный либо вычисленный трёхдневный срок; отсутствующая строка или trial без срока завершаются fail-closed. Legacy `manual` читается как совместимая active-запись, но runtime и админский UI используют только четыре продуктовых статуса. `validUntil` соответствует effective status.
- Удалена скрытая запись статуса, grace-даты и уведомлений из обычного `getSubscriptionRuntimeState`: owner и agent read-path больше не имеют побочных эффектов. Админская ручная корректировка нормализует даты так, чтобы выбранный статус после сохранения не превращался в другой effective status.
- Приём заявки согласован с публичной загрузкой: owner source проверяет подписку владельца; agent source — владельца и агента; collection source — владельца номера и creator context коллекции. Истёкший любой обязательный контекст возвращает `subscription_expired` до записи заявки.
- Обе owner-формы уже использовали общий server guard при создании активного номера и повторной активации. Миграция `202609150001_subscription_enforcement.sql` добавляет второй транзакционный барьер в PostgreSQL с advisory lock на владельца, поэтому параллельная вставка, повторная активация, property room и самостоятельный номер не обходят `active_room_limit`; DB-ошибка возвращается в тот же UI feedback `room-limit`.
- Ручное продление на 30 дней переведено на backend-only атомарную SQL-функцию: срок считается от более поздней даты — текущего `paid_until` или момента продления, статус становится active, grace очищается. В той же транзакции создаётся `subscription_audit_events` с администратором, прежним/новым статусом и сроком, числом дней и источником. Админская форма возвращается на `/admin/subscriptions`; UI прямо объясняет ограничение, способ продления и журналирование.
- Subscription owner/agent surface перенесён с legacy `br-*` на Tailwind CSS v4 с существующими `Panel`, `InlineNotice`, `StatusPill`, `StatCard` и `ButtonLink`. Новых reusable-компонентов и глобальных токенов не добавлено; legacy CSS не удалялся, потому что его ещё используют agent/collection/notification экраны следующих задач.
- Проверки: 31 целевой тест статусов, дат, лимитов, owner/agent parity, source-aware request gating, нормализации админских дат и ручного продления — passed; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint 16 TS/TSX-файлов — exit 0; `npm run build` — exit 0, компиляция 7.7 s и генерация 49/49 страниц; `npm run check:encoding` — exit 0, 373 tracked-файла; новые файлы отдельно подтверждены как UTF-8 without BOM; `git diff --check` — exit 0.
- Изолированный PostgreSQL smoke после цепочки миграций подтвердил блокировку двух способов превышения лимита и ручное продление owner/agent с двумя audit-записями; тест завершился rollback, временный контейнер удалён, существующие локальная и удалённая базы не менялись. Не относящиеся к подписке старые photo-миграции потребовали бы storage schema для полностью чистой vanilla-базы, но subscription migration и smoke прошли.
- Browser smoke production-сборки: owner expired-состояние проверено на mobile и 1440×1000 без горизонтального переполнения; кабинет объясняет read-only режим и лимит 5/5, `/p/Birusova` скрыт, request route сообщает, что новые заявки не принимаются. Текущая сохранённая browser-сессия имеет owner-role, поэтому agent route корректно вернул в owner кабинет; agent runtime/state/renewal подтверждены unit- и SQL-smoke без изменения внешних аккаунтов.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 321 файл, 1664 узла, 5583 связи, 77 сообществ. Предыдущие изменения задач 01–09 сохранены. Блокеров нет. Следующая задача: 11 «Агентский контур», файл `docs/rebuild-execution/tasks/11-agent.md`; её реализация в этом чате не начиналась.

## Результат задачи 11 — 2026-09-15

- Продуктовая опора: PRD 4.2, 5.4, 5.5, 5.7, 6, 7.3, 14 и 17. Сохранён персональный агентский контур без общего каталога, автоматического подтверждения проживания, оплаты проживания и редактирования данных владельца.
- Стабильный `agent_public_id` формата `ag_xxxxxx` назначается автоматически при роли агента, защищён от ручного изменения и используется в каноническом URL `/a/ag_xxxxxx`. Legacy slug редиректит на канонический URL с сохранением query-параметров даже при недоступной подписке; во временно недоступном состоянии контакты не раскрываются.
- Предложение проверяет роль агента, целевого владельца, разрешение объекта или самостоятельного номера и допустимый переход. Только целевой владелец принимает или отклоняет предложение; повторная отправка разрешена лишь после declined/revoked. DB-триггеры и строгие RLS-политики дублируют эти границы.
- Активный агент читает только разрешённые объект, номер, фото, цены и календарь. Изменение чужого объекта, номера, фото, базовой цены и занятости блокируется. Агент управляет только собственной надбавкой в диапазоне 0–999,99%; публичная витрина не сериализует контакты владельца.
- Агентская заявка повторно проверяет канонический публичный ID, роль, активное сотрудничество, подписки и room scope; базовая цена, сезонные цены и надбавка перечитываются на сервере, а source и pricing snapshot сохраняются. Агент передаёт только новую заявку и после принятия владельцем может единожды запросить завершение; статус `completed` выставляет только владелец.
- Agent dashboard, настройки, предложения, сотрудничества, read-only календарь, заявки, сделки и owner-экран предложений перенесены на Tailwind CSS v4 с существующими shared UI. Контакты и варианты сотрудничества вынесены в переиспользуемый `CollaborationDetails`, зарегистрированный в component index; legacy UI следующих задач 12–14 не удалялся.
- Проверки: 45 целевых тестов переходов, доступа и агентской цены — passed; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint — exit 0; `npm run build` — exit 0, 49/49 страниц; `npm run check:encoding` — exit 0, 373 tracked-файла; `git diff --check` — exit 0.
- Изолированный PostgreSQL smoke после полной цепочки миграций подтвердил выдачу ID, proposal accept/decline, read-only объект/фото/календарь, собственную надбавку, передачу заявки, запрос завершения и owner-only completed с неизменным snapshot. Тест завершился rollback; временный контейнер удалён, существующие базы не менялись.
- Browser smoke production-сборки пройден на 390×844 и 1440×1000 без горизонтального переполнения: owner-экран предложений доступен в сохранённой owner-сессии, попытка открыть agent dashboard корректно возвращает владельца в его кабинет, публичная агентская ссылка показывает subscription-expired state, legacy `/a/agent` редиректит на `/a/ag_aa3f05` с сохранением фильтров. В настроенных данных нет агента с действующей подпиской, поэтому авторизованный agent dashboard и активная витрина не проверялись через браузер без создания внешнего аккаунта; эти ветви подтверждены unit- и SQL-smoke.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 324 файла, 1674 узла, 5688 связей, 62 сообщества. Предыдущие изменения задач 01–10 сохранены. Блокеров нет. Следующая задача: 12 «Коллекции», файл `docs/rebuild-execution/tasks/12-collections.md`; её реализация в этом чате не начиналась.

## Результат задачи 12 — 2026-09-15

- Продуктовая опора: PRD 5.6, 6, 7 и 20. Коллекция остаётся персональной бессрочной подборкой владельца или агента до архивации; общего каталога, поиска по владельцам, рекомендаций, оплаты проживания и автоматического подтверждения заявки не добавлено.
- Создание и переименование поддерживают отдельные «Название в кабинете» и необязательное «Название для гостя». Владелец может включать только собственные объекты и самостоятельные номера, агент — собственные либо доступные по активному сотрудничеству. Архивная коллекция доступна только для чтения; изменения дополнительно блокируются при истёкшей подписке.
- Публичная страница разворачивает добавленный целиком объект в его активные номера, отдельно показывает добавленные номера и всегда ведёт к заявке на конкретный `room_id`. При открытии рассчитывается актуальная базовая или сезонная цена; для agent-коллекции применяется текущая надбавка и контакт агента. Перед записью заявки сервер повторно проверяет slug, роль, видимость, архив, состав коллекции, владельца номера, активное сотрудничество и подписки, затем сохраняет source/context и ценовой snapshot.
- Статистика открытий вынесена из server render в клиентский best-effort tracker. Одинаковый UUID сессии вкладки дедуплицируется уникальным событием в PostgreSQL, а atomic RPC увеличивает счётчик только при новом событии. Сбой аналитики не скрывает публичную страницу и не подменяется фиктивным успехом.
- Миграция `202609150003_collections_hardening.sql` добавляет ограничения данных, строгие RLS/триггеры для owner/agent scope и архивного состояния, canonical agent markup, проверку collection request context и atomic open events. Изолированный PostgreSQL smoke после полной цепочки миграций подтвердил owner/agent creation, запрет чужого inventory и подмены роли/наценки, сохранение request context/snapshot, дедупликацию открытий и read-only архив; транзакция откатилась, временный контейнер удалён, настроенные базы не менялись.
- Collection list/create/detail, публичная страница и request notice перенесены с legacy `br-*` на Tailwind CSS v4 с существующими `Panel`, `InlineNotice`, `StatCard`, `PublicStayFilter` и `BottomSheet`. Нового shared-компонента не потребовалось; удалены неиспользуемые collection/public-collection CSS-селекторы.
- Проверки: `npm run test:unit` — 90 passed / 0 failed; `npx tsc --noEmit` — exit 0; целевой ESLint 21 TS/TSX-файла — exit 0; `npm run build` — exit 0, 49/49 страниц; `npm run check:encoding` — exit 0, 373 tracked-файла; `git diff --check` — exit 0.
- Browser smoke production-сборки: owner list/detail и мобильная раскладка 390×844 проверены без горизонтального переполнения и ошибок консоли; public owner и agent ссылки корректно показали недоступность и не приняли заявку при истёкшей подписке. В настроенных данных нет owner/agent коллекции с действующей подпиской, поэтому активная публичная витрина не проверялась через браузер без изменения внешних данных; её ветви подтверждены unit- и SQL-smoke.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 327 файлов, 1693 узла, 5764 связи, 65 сообществ.
- Предыдущие изменения задач 01–13 сохранены. Блокеров нет. Следующая задача: 14 «Рефералы и админка», файл `docs/rebuild-execution/tasks/14-referrals-admin.md`; её реализация в этом чате не начиналась.

## Результат задачи 13 — 2026-09-15

- Продуктовая опора: PRD 9, 10, 13.5 и 17. Все базовые продуктовые события создают in-app запись и fan-out в PWA push / Telegram; SMS и новые продуктовые события не добавлялись.
- Миграция `202609150004_notifications_pwa_hardening.sql` добавляет обязательный `idempotency_key` и уникальность события на получателя, тип и бизнес-источник. Delivery-записи имеют стабильный `delivery_target_key`, уникальны на уведомление/канал/endpoint или чат и обновляются через upsert со статусами `sent`, `pending_configuration`, `skipped_*` или `failed`.
- Отказ push или Telegram не выбрасывается из fan-out и не превращает уже сохранённую заявку, передачу, предложение или изменение подписки в ошибку. Неожиданный сбой канала и невозможность записать delivery-status попадают в структурированную серверную диагностику; повторная обработка не дублирует in-app событие или delivery-запись.
- Все ссылки уведомлений рассчитываются сервером по типу события и owner/agent context; произвольный внешний URL из payload не используется. Тексты содержат только название объекта/номера и не включают имя, телефон, email или комментарий гостя. Legacy agent-ссылки in-app сохранены через строгий whitelist.
- Реальная push-доставка подключена через `web-push` и VAPID. Браузер проверяет поддержку и permission, явно регистрирует `/sw.js`, повторно использует существующую подписку и удаляет серверный endpoint до локальной отписки; ответы 404/410 удаляют устаревший endpoint. Сохранение push больше не включает обратно Telegram, а создание настроек не перезаписывает отключённые каналы.
- Telegram можно выключить и снова включить без удаления привязки чата; UI больше не показывает внутренний chat id. Привязка, отключение и повторная привязка сохраняют настройки других каналов.
- Manifest содержит отдельные фактические PNG 192×192, 512×512, maskable 512×512 и Apple 180×180. Service worker обрабатывает push/click, ограничивает переходы текущим origin и публикует диагностический статус регистрации. Центр уведомлений и push-карточка перенесены с legacy `br-*` на Tailwind и существующие `Panel`, `InlineNotice`, `StatusPill`, `ButtonLink` и `SubmitButton`; повреждённая кириллица исправлена, notification-specific CSS удалён.
- Проверки: `npm run test:unit` — 95 passed / 0 failed, включая 5 целевых тестов fan-out, role-safe links, idempotency и SW origin guard; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint 23 файлов — exit 0; `npm run build` — exit 0, 50/50 страниц; `npm run check:encoding` — exit 0, 373 tracked-файла; новые текстовые файлы проверены отдельно как UTF-8 without BOM; `git diff --check` — exit 0.
- Browser PWA smoke production-сборки: 390×844, manifest link найден, `serviceWorkerStatus=registered`, горизонтального переполнения нет, console warning/error пуст; manifest и `/sw.js` вернули 200, SW содержит push/click и same-origin guard, PNG-иконки ответили 200 с фактическими размерами 192×192, 512×512 и 180×180. Сохранённой auth-сессии в доступном in-app браузере не было, поэтому реальный provider send и изменение browser permission не выполнялись; без внешних VAPID/Telegram secrets delivery корректно остаётся `pending_configuration`, а permission/subscription ветви покрыты кодом и unit-тестами.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 329 файлов, 1707 узлов, 5837 связей, 65 сообществ. Предыдущие изменения задач 01–12 сохранены. Блокеров нет. Следующая задача: 14 «Рефералы и админка», файл `docs/rebuild-execution/tasks/14-referrals-admin.md`; её реализация в этом чате не начиналась.

## Результат задачи 14 — 2026-09-15

- Продуктовая опора: PRD 4.4, 5.1, 5.8, 8 и 8.1, а также решение 2026-06-11 из decision log. Сохранена персональная реферальная модель без каталога агентов, marketplace-логики, автоматической оплаты или автоматического подтверждения проживания.
- Миграция `202609150005_referrals_admin_hardening.sql` закрепляет одно активное приглашение на пригласившего, его роль, ожидаемую роль и intent; проверяет роль пригласившего, запрещает подмену роли и повторную привязку, атомарно и идемпотентно связывает регистрацию. Целевое действие создаётся автоматически только после фактического объекта/самостоятельного номера владельца либо активного сотрудничества агента и только при совпадении ожидаемой роли.
- Администратор подтверждает или отклоняет pending-бонус через атомарный RPC с блокировками и явными `already_*`/`conflict_*` результатами. Одно подтверждение ровно на 10 дней продлевает каждый существующий owner/agent контекст пригласившего и создаёт audit-запись с источником referral reward; повторное решение — no-op, отклонение подписку не меняет. Обычным пользователям запрещены выдача admin-роли, изменение наград и выполнение административных RPC.
- Регистрация проверяет приглашение до создания аккаунта, фиксирует ожидаемую роль и потребляет ссылку идемпотентно даже при уже созданном профиле. Невалидная, использованная, отозванная или просроченная ссылка не проходит регистрацию. Очередь модерации, referral share/card, invite page и админские разделы перенесены с legacy `br-*` на Tailwind CSS v4 с существующими `Panel`, `InlineNotice`, `StatusPill`, `Button`, `ButtonLink`, `Tabs` и `AuthShell`; нового reusable-компонента и изменения component index не потребовалось.
- Админские hide/freeze используют отдельные role-checked RPC, возвращают явные no-op состояния и после изменения перевалидируют соответствующие публичные страницы. Ошибки чтения очереди и неожиданные состояния больше не маскируются пустыми данными; подтверждение создаёт идемпотентные уведомления для обоих контекстов пригласившего.
- Проверки: `npm run test:unit` — 98 passed / 0 failed; `npx --no-install tsc --noEmit --incremental false` — exit 0; целевой ESLint 17 TS/TSX-файлов — exit 0; `npm run build` — exit 0, 50/50 страниц; `npm run check:encoding` — exit 0, 372 tracked text files; `git diff --check` — exit 0.
- Изолированный PostgreSQL smoke после полной цепочки миграций подтвердил уникальность приглашения, запрет role substitution и reassignment, owner/agent milestones, запрет admin-операций обычному пользователю, ровно два десятидневных продления для двух контекстов, отсутствие повторного начисления, безопасное отклонение, hide/freeze и их no-op повторы. Транзакция откатилась, временный контейнер удалён, настроенные базы не менялись.
- Browser smoke production-сборки: невалидное приглашение проверено на 390×844 и 1440×1000 без горизонтального переполнения и console warning/error; `/admin/reviews` без сессии корректно перенаправляет на `/login`. В доступном in-app браузере не было admin-сессии, поэтому наполненная очередь визуально не проверялась без создания или изменения внешнего аккаунта; её данные, переходы и повторные решения подтверждены сборкой, unit- и SQL-smoke.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 329 файлов, 1706 узлов, 5866 связей, 67 сообществ. Предыдущие изменения задач 01–13 сохранены. Блокеров нет. Следующая задача: 15 «Очистка и финальная проверка», файл `docs/rebuild-execution/tasks/15-final-cleanup.md`; её реализация в этом чате не начиналась.

## Результат задачи 15 — 2026-09-15

- Продуктовая опора: PRD 2, 3, 14, 15 и 17. Продуктовые роли, границы и бизнес-логика не расширялись; общего каталога, marketplace, оплаты проживания и автоматического подтверждения не добавлено.
- Статический аудит импортов подтверждает ноль нарушений направления `shared → entities → features → widgets → app`. Auth/referral side effect отделён от shared profile bootstrap; sign-out и admin actions находятся в feature-слое; календарные helpers перенесены из widget в entity.
- Удалены только доказанно неиспользуемые legacy widgets календаря, комнат, инвентарной кнопки, property overview, дублирующий form accordion и старый calendar-block mock. Неиспользуемый `getRequestRoom` с прямой зависимостью от room mock удалён; оставшиеся property/room/request mocks доступны только через явно включаемый `BRONLY_DEMO_MODE=true` read-only сценарий.
- Все живые UI-потребители `br-*` перенесены на Tailwind CSS v4 и существующие shared primitives. `globals.css` сокращён с 6045 до 319 строк: остались reset, глобальные theme tokens, dashboard scope и общие keyframes. `component-index.md` и `local-development.md` согласованы с фактическими компонентами и архитектурными правилами.
- Общий формат рублей вынесен в `shared/lib`; ручные приведения к доменным enum заменены runtime guards в затронутых местах. Нет `as any`, `as unknown`, `as never` и пустых catch. Удаление ошибочного cast выявило и исправило неполную Supabase projection standalone-номера: запрос агентского предложения теперь выбирает `price_per_night` и проверяет `room_kind` fail-closed.
- Полные проверки: `npm run test:unit` — 98 passed / 0 failed; `npm run lint` — exit 0; `npx --no-install tsc --noEmit --incremental false` — exit 0; `npm run build` — exit 0, 50/50 страниц; `npm run check:encoding` — exit 0, 358 tracked text files; `git diff --check` — exit 0.
- Пять SQL smoke-файлов прошли после полной цепочки миграций в новой одноразовой Supabase PostgreSQL-базе: access, subscription, agent workflow, collections и referral/admin. Legacy access fixture согласован с новым agent-room write-trigger; проверяемые RLS-политики не отключались. Транзакции откатились, контейнер удалён, настроенный Supabase и production не менялись.
- Browser smoke production-сборки пройден на 390×844 и 1440×1000: landing/auth, guest expired owner page/request, неизвестные agent/collection ссылки и role-gated owner/agent/admin маршруты без сессии. Горизонтального переполнения и console warning/error нет; новые заявки на expired-странице не принимаются, закрытые кабинеты fail-closed перенаправляют на login. Авторизованных agent/admin-сессий в доступном браузере не было, поэтому наполненные role-сценарии подтверждены unit/build и SQL smoke без изменения внешних аккаунтов.
- Graphify: финальный `graphify update ./src` — exit 0, AST-only; 320 файлов, 1681 узел, 5870 связей, 60 сообществ. Следующей pending-задачи нет; новый чат не создаётся.

## Правило обновления

После каждой задачи записать сюда основные измененные области, выполненные проверки, известные ограничения и номер следующей задачи.

## Правило передачи

- Один чат выполняет одну задачу.
- Следующий чат создается только после сохранения результата и обновления этого файла.
- Все чаты работают последовательно в локальном checkout этого проекта.
- В новый чат передается NEXT_TASK_PROMPT.md с номером и путем следующей задачи.
- История предыдущего чата не копируется.
