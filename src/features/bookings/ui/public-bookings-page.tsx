import { Card } from "@/shared/ui/card";

export function PublicBookingsPage() {
  return (
    <Card className="p-5 text-sm text-muted-foreground">
      Общий публичный календарь отключен. Публичные страницы будут доступны через подборки.
    </Card>
  );
}
